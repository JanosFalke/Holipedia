/**************************PASSWORD FOR FRENCH !!!INDEX!!!************************************/
function passWord() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Entrez mot de passe');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('Bon mot de passe!');
          window.open('html/premiumArea.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaies)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaie)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";  
    }


/**************************PASSWORD FOR GERMAN PAGES (+INDEX)************************************/
function passWordDeutsch() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Passwort eingeben');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('Richtiges Passwort!');
          window.open('premiumBereich.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übrige Versuche)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übriger Versuch)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }


/**************************PASSWORD FOR ENGLISH PAGES (+INDEX)************************************/
function passWordEnglish() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Please enter Passwort');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('You got it right!');
          window.open('premiumArea.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übrige Versuche)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übriger Versuch)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }


/**************************PASSWORD FOR FRENCH PAYS _PAGES************************************/
function passWordsousDossier() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Entrez mot de passe');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('Bon mot de passe!');
          window.open('premiumArea.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaies)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaie)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }


/**************************PASSWORD FOR FRENCH VILLES _PAGES************************************/
function passWordsousSousDossier() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Entrez mot de passe');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('Bon mot de passe!');
          window.open('../premiumArea.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaies)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Mauvais mot de passe. ('+testReste+' essaie)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }



/**************************PASSWORD FOR GERMAN VILLES _PAGES************************************/
function passWordsousDossierDE() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Passwort eingeben');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('Richtiges Passwort!');
          window.open('../premiumBereich.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        
        if(testReste>1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übrige Versuche)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übriger Versuch)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }


/**************************PASSWORD FOR ENGLISH VILLES _PAGES************************************/
function passWordsousDossierEN() {
    let testV = 1;
    let testReste = 4;
    let pass1 = prompt('Please enter Passwort');
        while (testV < 5) {
        if (pass1.toLowerCase() == "holipedia2017") {
          alert('You got it right!');
          window.open('../premiumArea.html', "_self");
          break;
        } 
        if(pass1 === ""){  
            return " ";
        }         
        testV++;
        testReste = testReste-1;
        if (testReste > 1) {
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übrige Versuche)');
        }
        else if (testReste==1){
            let pass1 = prompt('Zugang verweigert - Falsches Passwort. ('+testReste+' übriger Versuch)');  
        }
            
        }      
      if (pass1.toLowerCase() != "password" & testV == 5)
      {

      }
      return " ";
    }


